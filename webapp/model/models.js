// this file is referenced in the Component.js file, although I don't know what it is or where it came from.  Won't delete it yet, since I don't know what it does and where the code came from.

/*
	sap.ui.define([
		"sap/ui/model/json/JSONModel",
		"sap/ui/Device"
	], function (JSONModel, Device) {
		"use strict";

		return {

			createDeviceModel: function () {
				var oModel = new JSONModel(Device);
				oModel.setDefaultBindingMode("OneWay");
				return oModel;
			}

		};
	});


*/